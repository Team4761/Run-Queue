package org.robockets.runqueue.client.controllers;

/**
 * All controllers declarations are statically placed here so all other classes have
 * a common way to call into a controller method
 */
public class Controllers {
	public static MainController main = new MainController();
}
