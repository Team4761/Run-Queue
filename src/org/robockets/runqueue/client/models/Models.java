package org.robockets.runqueue.client.models;

/**
 * All model declarations are statically placed here so all other classes have
 * a common way to call into a model method
 */
public class Models {
	public static QueueModel queue = new QueueModel();
}
